
      <table>
        <thead>
          <tr>
            <th>name</th>
            <th>code</th>
          </tr>
        </thead>
        <tbody>
          <% @companies.each do |company| %>
            <tr>
                <td><%= company.name %></td>
                <td><%= company.code %></td>
            </tr>
        <% end %>
        </tbody>
      </table>
      <%= will_paginate @companies %>
